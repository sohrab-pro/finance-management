import {View, Text, StyleSheet} from 'react-native';

const Login = () => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Login;
